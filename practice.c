#include <stdio.h>
#include<string.h>
#define loop int t;scanf("%d",&t);while(t--)
typedef long long ll;

void init_code() {
#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif
}

int main(){
    init_code();
    printf("Hello World\n");
    return 0;
}
